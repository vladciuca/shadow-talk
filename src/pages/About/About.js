import React from "react";
import { Screen, AboutHeader, AboutContent } from "../../components";

const About = () => (
  <Screen header={<AboutHeader />} content={<AboutContent />} />
);

export default About;
